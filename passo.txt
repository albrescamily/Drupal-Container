docker cp "C:\Users\camily albres\t1-cloud-computing\scripts\site-configure.sh" drupal-t1:/opt/drupal

docker exec -it t1-drupal bash

./configure.sh

WORKDIR /var/www/html






docker-compose up --build
docker-compose up -d

















# Usando uma imagem base do Drupal
FROM drupal:latest

# Instalar o Composer
RUN curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer

# Instalar o Drush globalmente
RUN composer require drush/drush

RUN drush --version 

# # Instalar o Drupal via Composer
# RUN composer create-project drupal/recommended-project /opt/drupal/web

# # Rodar o Drush para configurar o Drupal
# RUN cd /opt/drupal/web && drush site:install --db-url=mysql://drupaluser:drupalpass@db/drupal --site-name="Cloud Computing" --account-name=admin --account-pass=admin --yes

# # Verificar a versão do Drush
# RUN drush --version

#RUN chmod -R 777 /var/www/html
#posso criar um usuario no meu host para ter permissoes de acesso no meu volume
#RUN chown -R www-data:www-data /var/www/html

#COPY ./scripts/site-configure.sh /var/www/html/scripts/site-configure.sh
#COPY ./scripts/site-configure.sh /var/www/html/scripts/site-configure.sh

#RUN chmod +x /var/www/html/scripts/site-configure.s

#ENTRYPOINT ["bash", "scripts\site-configure.sh"]


#nao respeita o workdir. Ele procura o $PATH do sistema
#podemos executar a configuração do meu drupal de dentro do container ou via host chamando exec e paramentros 



